import { AllCategoryDirective } from './all-category.directive';

describe('AllCategoryDirective', () => {
  it('should create an instance', () => {
    const directive = new AllCategoryDirective();
    expect(directive).toBeTruthy();
  });
});
